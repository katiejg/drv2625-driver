description: GPIO pin to switch a driver VDD on or off

compatible: "drv-switch"

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      The GPIO connected to the switch that controls DRV VDD.
